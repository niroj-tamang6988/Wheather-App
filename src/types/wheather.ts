export interface location{
    name: string;
    country: string;
}

export interface current_Weather{
    name: string;
    temp_c: number;
    day: string;

}

export interface forecast{
    day: string;
    date: number;

}